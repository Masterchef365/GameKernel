Currently, modules poll all handles instead of just the woken ones (probably a small perf hit)
Modules cannot be killed
Modules can use as much memory and processing time as they'd like
Single bytes are sent over channels (probably a huge performance hit)
Some bug with message lengths?
